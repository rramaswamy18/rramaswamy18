@using ArchitectureLibraryCacheData
@using RetailSlnCacheData
@using RetailSlnModels
@model UserAddEditModel
<style>
    #EmailAddress-error, #TelephoneCountryId-error, #TelephoneNumber-error, #FirstName-error, #LastName-error,
    #CorpAcctId-error, #UserTypeId-error {
        color: #ff0000;
        display: block;
        font-size: 13px;
        font-weight: bold;
    }
</style>
@{
    bool mobileDevice = Request.Browser.IsMobileDevice;
    //mobileDevice = true;
    if (mobileDevice)
    {
        @Html.Partial("_UserAddEdit2", Model)
    }
    else
    {
        @Html.Partial("_UserAddEdit1", Model)
    }
}

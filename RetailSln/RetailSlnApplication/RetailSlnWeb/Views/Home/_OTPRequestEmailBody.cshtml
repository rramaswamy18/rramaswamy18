@using ArchitectureLibraryCacheData
@using ArchitectureLibraryModels
@model OTPResponseModel
@{
    long clientId = ArchLibCache.ClientId;
}
@(ArchLibCache.GetApplicationDefault(clientId, "BusinessNameAbbrev", "") + " OTP " + Model.OTPCode + " expires in " + Model.OTPDuration + " minutes at " + Model.OTPExpiryDate + " " + Model.OTPExpiryTime)
